# Frontend Environment Variables for LOCAL DEVELOPMENT

# API URL Configuration
# Local development: http://localhost:5001
# Production: Leave this commented out (will use /api which is same domain)
VITE_API_URL=http://localhost:5001

# For production deployment, comment out or remove VITE_API_URL
# The app will automatically use /api (relative path)